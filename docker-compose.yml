services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:80 | grep -q '<div id=\"root\"></div>'"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
