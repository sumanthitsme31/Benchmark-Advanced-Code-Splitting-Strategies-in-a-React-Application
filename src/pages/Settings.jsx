export default function Settings() {
  return (
    <section data-testid="settings-page">
      <h2 data-testid="settings-title">Settings</h2>
      <p data-testid="settings-description">Lightweight user preferences page.</p>
    </section>
  )
}
